{% extends 'layout.twig' %}

{% block title %}
    Novice | {{ app.config.get('general/sitename') }}
{% endblock %}

{% block slides %}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-8">
                {% for record in records %}
                    <div class="row">
                        <div class="col-lg-12">
                            <article>
                                <h2>{{ record.title|upper }}</h2>
                                <p>Objavljeno: {{ record.datepublish|date("d.m.Y") }} </p>
                                <p><a href="{{ record.link }}">Preberi več</a></p>
                                <p></p>
                            </article>
                        </div>
                    </div>
                {% endfor %}

                {{ pager() }}
            </div>
            <div class="col-lg-4">
                {% include '_aside.twig' %}
            </div>
        </div>
    </div>
{% endblock %}